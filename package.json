{
  "name": "swarm-mutable-batch-test",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "description": "Empirically tests whether Swarm mutable postage batches have any observable effect (SOC overwrite, read from the proximity-determined storer node).",
  "scripts": {
    "cluster:start": "bee-compose start --full 4",
    "cluster:stop": "bee-compose stop",
    "test": "tsx mutable-batch-test.ts",
    "test:divergence": "tsx same-stamp-divergence-test.ts"
  },
  "dependencies": {
    "@ethersphere/bee-js": "^11.1.1"
  },
  "devDependencies": {
    "@snaha/bee-compose": "^0.1.4",
    "@types/node": "^22",
    "tsx": "^4.20.0",
    "typescript": "^5"
  }
}
